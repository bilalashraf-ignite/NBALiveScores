---
phase: 04-game-details-statistics
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/types/sports-data.ts
  - src/components/historical-matchup.tsx
  - src/components/game-detail-modal.tsx
  - src/lib/adapters/balldontlie-adapter.ts
  - src/lib/adapters/ncaa-adapter.ts
  - src/lib/adapters/euroleague-adapter.ts
  - tests/types/sports-data.test.ts
  - tests/components/historical-matchup.test.tsx
  - tests/integration/game-detail-modal.test.tsx
autonomous: true
requirements:
  - HIST-03
  - HIST-04
user_setup: []

must_haves:
  truths:
    - "User can view last 5 meetings between two teams with dates and scores"
    - "User can see season series record (e.g., 'Team A leads 2-1 this season')"
    - "User can see head-to-head all-time record"
    - "Fallback message shown when historical data unavailable"
  artifacts:
    - path: "src/types/sports-data.ts"
      provides: "HistoricalMatchup interface with last 5 meetings, season series, all-time record"
      contains: "interface HistoricalMatchup"
    - path: "src/components/historical-matchup.tsx"
      provides: "Historical matchup display with list format and text summaries"
      min_lines: 60
    - path: "tests/components/historical-matchup.test.tsx"
      provides: "Tests for last 5 display, season series, all-time record, unavailable fallback"
      exports: []
  key_links:
    - from: "src/components/game-detail-modal.tsx"
      to: "HistoricalMatchup component"
      via: "renders component when data available"
      pattern: "&lt;HistoricalMatchup.*historicalMatchup"
    - from: "src/components/historical-matchup.tsx"
      to: "date-fns"
      via: "format dates for last 5 meetings"
      pattern: "format.*date.*fns"
---

<objective>
Create historical matchup data display showing last 5 meetings, season series record, and all-time record with honest fallback messaging when data unavailable.

Purpose: Provide users with historical context for current matchup (HIST-03, HIST-04).
Output: Historical matchup section at bottom of modal with list of recent games and text summaries of series records.
</objective>

<execution_context>
@/Users/bilalashraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bilalashraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-details-statistics/04-CONTEXT.md
@.planning/phases/04-game-details-statistics/04-RESEARCH.md
@.planning/phases/04-game-details-statistics/04-VALIDATION.md
@.planning/phases/04-game-details-statistics/04-01-PLAN.md
@.planning/phases/04-game-details-statistics/04-02-PLAN.md
@.planning/phases/04-game-details-statistics/04-03-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/sports-data.ts (created in Plan 01):
```typescript
export interface GameDetails {
  gameId: string;
  league: League;
  homeTeam: Team;
  awayTeam: Team;
  teamStats: { home: TeamStats; away: TeamStats };
  playerStats: { home: PlayerStats[]; away: PlayerStats[] };
  historicalMatchup: HistoricalMatchup;  // Will define in this plan
}
```

From CONTEXT.md Historical Matchup Data section:
- Last 5 meetings (date, score, winner)
- Season series record (e.g., "Team A leads 2-1 this season")
- Head-to-head all-time record (e.g., "Team A leads 45-32 all-time")
- Average combined points per matchup
- "Historical data unavailable" message if no data
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define HistoricalMatchup interface and populate mock data</name>
  <files>
    src/types/sports-data.ts
    src/lib/adapters/balldontlie-adapter.ts
    src/lib/adapters/ncaa-adapter.ts
    src/lib/adapters/euroleague-adapter.ts
    tests/types/sports-data.test.ts
  </files>
  <behavior>
    - HistoricalMatchup interface includes last 5 meetings, season series, all-time record, average points
    - All fields are optional to support graceful degradation when data unavailable
    - Mock adapters return realistic historical data for known rivalries
  </behavior>
  <action>
    1. Add HistoricalMatchup interface to src/types/sports-data.ts:
       ```typescript
       export interface HistoricalMatchup {
         lastFiveMeetings?: Array&lt;{
           date: string;           // ISO date format "2024-12-15"
           homeTeam: string;       // Team name
           awayTeam: string;       // Team name
           homeScore: number;
           awayScore: number;
           winner: 'home' | 'away';
         }&gt;;
         seasonSeries?: {
           wins: number;           // Current season wins for one team
           losses: number;         // Current season losses for one team
           leader: 'home' | 'away' | 'tied';
         };
         allTimeRecord?: {
           wins: number;           // All-time wins for one team
           losses: number;         // All-time losses for one team
           leader: 'home' | 'away';
         };
         averageCombinedPoints?: number;
       }
       ```

    2. Update BalldontlieAdapter.getGameDetails() with realistic NBA historical data:
       - Lakers vs Celtics rivalry:
       - Last 5 meetings: dates from last 2 seasons, alternating wins, realistic scores (105-112, 98-95, etc.)
       - Season series: Lakers leads 2-1 this season (leader: 'home')
       - All-time record: Celtics lead 163-127 all-time (leader: 'away')
       - Average combined points: 208

    3. Update NcaaAdapter with realistic NCAA rivalry data (Duke vs UNC):
       - Last 5 meetings from last 2 seasons
       - Season series: tied 1-1 or one team leads
       - All-time record: UNC leads historically
       - Average combined points: typical college game total (145-155)

    4. Update EuroLeagueAdapter with realistic EuroLeague rivalry data (Real Madrid vs Barcelona):
       - Last 5 meetings from recent seasons
       - Season series: Barcelona leads 2-0
       - All-time record: Real Madrid leads (EuroLeague era)
       - Average combined points: typical EuroLeague total (160-170)

    Note: Mock data per CONTEXT.md decision (defer real API integration).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="types/sports-data" -x</automated>
  </verify>
  <done>HistoricalMatchup interface defined with all specified fields, all fields optional for graceful degradation, adapters return realistic historical rivalry data, tests verify structure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create HistoricalMatchup component with list and text summaries</name>
  <files>
    src/components/historical-matchup.tsx
    tests/components/historical-matchup.test.tsx
  </files>
  <behavior>
    - Component displays last 5 meetings in list format (date, teams, score, winner)
    - Season series and all-time record shown as text summaries
    - Average combined points displayed
    - Fallback "Historical data unavailable" message when data is null/undefined or lastFiveMeetings is empty
    - Date formatting using date-fns (already installed per Phase 3)
  </behavior>
  <action>
    1. Create src/components/historical-matchup.tsx:
       - Import format from 'date-fns' for date formatting
       - Accept props: data: HistoricalMatchup | null | undefined, homeTeam: Team, awayTeam: Team
       - Check if data is unavailable: !data || !data.lastFiveMeetings || data.lastFiveMeetings.length === 0
       - If unavailable, render fallback:
         ```tsx
         &lt;div className="text-gray-600"&gt;
           &lt;p&gt;Historical data unavailable&lt;/p&gt;
         &lt;/div&gt;
         ```
       - If available, render:
         - Section heading: &lt;h3 className="text-lg font-semibold mb-3"&gt;Last 5 Meetings&lt;/h3&gt;
         - List of meetings (ul with space-y-2):
           - For each meeting: &lt;li className="flex justify-between text-sm"&gt;
           - Left side: formatted date (e.g., "Dec 15, 2024") using format(new Date(meeting.date), 'MMM d, yyyy')
           - Middle: teams and score (e.g., "Lakers 105, Celtics 112")
           - Right side: winner indicator (bolded winning team name or checkmark)
         - Season series summary (if data.seasonSeries exists):
           - &lt;p className="mt-4 text-sm text-gray-700"&gt;
           - Text: "Season series: {leaderTeamName} leads {wins}-{losses}" or "Season series: Tied {wins}-{wins}"
           - Use homeTeam.name and awayTeam.name based on leader field
         - All-time record summary (if data.allTimeRecord exists):
           - &lt;p className="mt-2 text-sm text-gray-700"&gt;
           - Text: "All-time: {leaderTeamName} leads {wins}-{losses}"
         - Average combined points (if data.averageCombinedPoints exists):
           - &lt;p className="mt-2 text-sm text-gray-600"&gt;
           - Text: "Average combined points: {averageCombinedPoints}"
       - Tailwind styling: border-t border-gray-200 pt-4 for section separation from player stats above

    2. Create tests/components/historical-matchup.test.tsx:
       - Test: displays "Last 5 Meetings" heading when data available
       - Test: all 5 meetings appear in list (verify 5 list items)
       - Test: dates are formatted correctly (check for "Dec 15, 2024" pattern)
       - Test: scores appear for each meeting (check for "-" separator in score)
       - Test: winner is indicated (bolded team name or visual indicator)
       - Test: season series summary appears with correct text (e.g., "Lakers leads 2-1")
       - Test: all-time record summary appears with correct text (e.g., "Celtics leads 163-127")
       - Test: average combined points appears
       - Test: fallback message "Historical data unavailable" when data is null
       - Test: fallback message when data.lastFiveMeetings is empty array
       - Test: fallback message when data exists but lastFiveMeetings is undefined
       - Use React Testing Library: render, screen.getByText, screen.getAllByRole('listitem')

    Note: date-fns already installed in project (used in Phase 3 for timezone handling).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="components/historical-matchup" -x</automated>
  </verify>
  <done>HistoricalMatchup component displays last 5 meetings with formatted dates, season series and all-time record summaries, average points, fallback message when data unavailable, tests verify all behaviors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate HistoricalMatchup into GameDetailModal</name>
  <files>
    src/components/game-detail-modal.tsx
    tests/integration/game-detail-modal.test.tsx
  </files>
  <behavior>
    - Modal displays historical matchup section at bottom after player stats
    - Section always present (shows fallback if data unavailable)
    - Integration tests verify full flow and data availability handling
  </behavior>
  <action>
    1. Modify src/components/game-detail-modal.tsx:
       - Import HistoricalMatchup
       - In the data display section (at the bottom, after player stats, where placeholder "Historical data coming in Plan 04" was):
         - Remove placeholder text
         - Add section heading: &lt;h2 className="text-xl font-bold mt-8 mb-4"&gt;Historical Matchup&lt;/h2&gt;
         - Always render HistoricalMatchup component (it handles its own fallback):
           ```tsx
           &lt;HistoricalMatchup
             data={data?.historicalMatchup}
             homeTeam={data.homeTeam}
             awayTeam={data.awayTeam}
           /&gt;
           ```
         - No conditional rendering (component shows "unavailable" message internally if needed)

    2. Update tests/integration/game-detail-modal.test.tsx:
       - Test: open modal for live game, verify historical matchup section heading appears
       - Test: verify "Last 5 Meetings" subheading appears (data is available in mock)
       - Test: verify at least one meeting date appears (check for formatted date like "Dec 15, 2024")
       - Test: verify season series summary appears (check for "leads" or "tied" text)
       - Test: verify all-time record summary appears
       - Test: scroll to bottom of modal, historical section is visible (not cut off)
       - Test: if adapter returned null historicalMatchup, "Historical data unavailable" message appears

    Note: Section always present per CONTEXT.md decision (honest about data limitations).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="integration/game-detail-modal" -x</automated>
  </verify>
  <done>HistoricalMatchup integrated at bottom of modal after player stats, section always present with fallback handling, integration tests verify last 5 meetings display, season series summary, all-time record, and unavailable state</done>
</task>

</tasks>

<verification>
Overall plan verification:

1. Open modal for live game → historical matchup section appears at bottom
2. Verify "Last 5 Meetings" subheading
3. Verify 5 meetings listed with dates (e.g., "Dec 15, 2024")
4. Verify scores for each meeting (e.g., "Lakers 105, Celtics 112")
5. Verify winner indicated for each meeting
6. Verify season series summary (e.g., "Season series: Lakers leads 2-1")
7. Verify all-time record summary (e.g., "All-time: Celtics leads 163-127")
8. Verify average combined points (e.g., "Average combined points: 208")
9. Test unavailable scenario: modify mock to return null historicalMatchup → verify "Historical data unavailable" message appears

Automated: `npm test -- --testPathPattern="04-04" -x` (all Task 1-3 tests)
</verification>

<success_criteria>
- User can view historical head-to-head matchup data between teams (HIST-03)
- User can see last 5 meetings with dates, scores, and winners (HIST-04)
- User can see season series record showing current season head-to-head
- User can see all-time record between the two teams
- User can see average combined points per matchup
- Fallback message appears when historical data is unavailable (honest about limitations)
- Historical section always present at bottom of modal (not conditionally hidden)
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-details-statistics/04-04-SUMMARY.md`
</output>
