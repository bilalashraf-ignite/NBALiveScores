---
phase: 04-game-details-statistics
plan: 02
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src/types/sports-data.ts
  - src/components/team-stats-table.tsx
  - src/components/game-detail-modal.tsx
  - src/lib/adapters/balldontlie-adapter.ts
  - src/lib/adapters/ncaa-adapter.ts
  - src/lib/adapters/euroleague-adapter.ts
  - tests/types/sports-data.test.ts
  - tests/components/team-stats-table.test.tsx
  - tests/integration/game-detail-modal.test.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
user_setup: []

must_haves:
  truths:
    - "User can view team field goal percentage with made-attempted format in game detail modal"
    - "User can view team rebounds broken down by offensive, defensive, and total"
    - "User can view team assists, turnovers, steals, and blocks"
    - "Better stat value is bolded for visual comparison"
  artifacts:
    - path: "src/types/sports-data.ts"
      provides: "TeamStats interface with all shooting, rebounding, and defensive stats"
      contains: "interface TeamStats"
    - path: "src/components/team-stats-table.tsx"
      provides: "Team comparison table with side-by-side format and bold highlighting"
      min_lines: 60
    - path: "tests/components/team-stats-table.test.tsx"
      provides: "Tests for all 10 stats display, bold highlighting, shooting percentage format"
      exports: []
  key_links:
    - from: "src/components/game-detail-modal.tsx"
      to: "TeamStatsTable component"
      via: "renders table when data available"
      pattern: "&lt;TeamStatsTable.*teamStats"
    - from: "src/components/team-stats-table.tsx"
      to: "TeamStats from GameDetails"
      via: "props.homeStats and props.awayStats"
      pattern: "homeStats.*TeamStats"
---

<objective>
Create team statistics display with comprehensive shooting, rebounding, and defensive metrics in side-by-side comparison format with bold highlighting for better values.

Purpose: Enable users to view detailed team performance statistics during live games (STAT-01, STAT-02).
Output: Team statistics comparison table showing all 10 specified stats with made-attempted + percentage format for shooting.
</objective>

<execution_context>
@/Users/bilalashraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bilalashraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-details-statistics/04-CONTEXT.md
@.planning/phases/04-game-details-statistics/04-RESEARCH.md
@.planning/phases/04-game-details-statistics/04-VALIDATION.md
@.planning/phases/04-game-details-statistics/04-00-PLAN.md
@.planning/phases/04-game-details-statistics/04-01-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/sports-data.ts (created in Plan 01):
```typescript
export interface GameDetails {
  gameId: string;
  league: League;
  homeTeam: Team;
  awayTeam: Team;
  status: GameStatus;
  score: Score;
  gameContext?: GameContext;
  teamFouls?: TeamFouls;
  teamStats: {
    home: TeamStats;  // Will define TeamStats in this plan
    away: TeamStats;
  };
  playerStats: { home: PlayerStats[]; away: PlayerStats[] };
  historicalMatchup: HistoricalMatchup;
}
```

From CONTEXT.md Team Statistics section:
- FG% (Field Goal Percentage) with made-attempted format
- 3P% (Three-Point Percentage) with made-attempted format
- FT% (Free Throw Percentage) with made-attempted format
- Assists, Turnovers
- Offensive Rebounds, Defensive Rebounds, Total Rebounds
- Steals, Blocks

Test files created in Plan 04-00:
- tests/types/sports-data.test.ts (RED tests including TeamStats interface)
- tests/components/team-stats-table.test.tsx (RED tests for table display and highlighting)
- tests/integration/game-detail-modal.test.tsx (RED integration tests)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define TeamStats interface and populate mock data</name>
  <files>
    src/types/sports-data.ts
    src/lib/adapters/balldontlie-adapter.ts
    src/lib/adapters/ncaa-adapter.ts
    src/lib/adapters/euroleague-adapter.ts
    tests/types/sports-data.test.ts
  </files>
  <behavior>
    - TeamStats interface includes all 10 specified stats per CONTEXT.md
    - Shooting stats have { made: number, attempted: number, percentage: number } structure
    - Rebound stats have offensive, defensive, and total fields
    - Mock adapters return GameDetails with realistic team stats
  </behavior>
  <action>
    1. Add TeamStats interface to src/types/sports-data.ts:
       ```typescript
       export interface TeamStats {
         fieldGoals: { made: number; attempted: number; percentage: number };
         threePointers: { made: number; attempted: number; percentage: number };
         freeThrows: { made: number; attempted: number; percentage: number };
         assists: number;
         turnovers: number;
         reboundsOffensive: number;
         reboundsDefensive: number;
         reboundsTotal: number;
         steals: number;
         blocks: number;
       }
       ```

    2. Update BalldontlieAdapter.getGameDetails() to return realistic NBA team stats:
       - Example: Lakers vs Celtics live game
       - Home team (Lakers): FG 38-82 (46.3%), 3P 12-35 (34.3%), FT 18-22 (81.8%)
       - Home: 8 AST, 12 TO, 10 OREB, 28 DREB, 38 TREB, 6 STL, 5 BLK
       - Away team (Celtics): FG 42-88 (47.7%), 3P 15-38 (39.5%), FT 14-18 (77.8%)
       - Away: 12 AST, 10 TO, 8 OREB, 32 DREB, 40 TREB, 8 STL, 6 BLK

    3. Update NcaaAdapter.getGameDetails() with realistic NCAA stats (Duke vs UNC)

    4. Update EuroLeagueAdapter.getGameDetails() with realistic EuroLeague stats (Real Madrid vs Barcelona)

    Note: Mock data per CONTEXT.md decision (defer real API integration).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="types/sports-data" -x</automated>
  </verify>
  <done>TeamStats interface defined with all 10 specified stats, adapters return GameDetails with realistic team stats, tests turn GREEN verifying structure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TeamStatsTable component with side-by-side comparison</name>
  <files>
    src/components/team-stats-table.tsx
    tests/components/team-stats-table.test.tsx
  </files>
  <behavior>
    - Table displays 10 stats in specified order (FG%, 3P%, FT%, AST, TO, OREB, DREB, TREB, STL, BLK)
    - Side-by-side format: Home value | Stat label | Away value
    - Shooting stats show "made-attempted, percentage%" format (e.g., "38-82, 46.3%")
    - Better value is bolded (higher percentage for FG/3P/FT, higher count for AST/REB/STL/BLK, lower count for TO)
    - Full words for rebound labels (Offensive Rebounds, Defensive Rebounds, Total Rebounds)
  </behavior>
  <action>
    1. Create src/components/team-stats-table.tsx:
       - Accept props: homeStats: TeamStats, awayStats: TeamStats, homeTeam: Team, awayTeam: Team
       - Define stats array with 10 entries in specified order per CONTEXT.md:
         1. FG% (Field Goal Percentage)
         2. 3P% (Three-Point Percentage)
         3. FT% (Free Throw Percentage)
         4. Assists
         5. Turnovers
         6. Offensive Rebounds
         7. Defensive Rebounds
         8. Total Rebounds
         9. Steals
         10. Blocks
       - Each stat entry has:
         - label: string (display name)
         - homeValue: (stats: TeamStats) => string (format function)
         - awayValue: (stats: TeamStats) => string (format function)
         - compareValue: (home: TeamStats, away: TeamStats) => number (returns positive if home is better, negative if away is better)
       - For shooting stats, format as "made-attempted, percentage%" (e.g., "38-82, 46.3%")
       - For compareValue: FG%/3P%/FT% compare percentage field, AST/REB/STL/BLK compare counts (higher is better), TO compare counts (lower is better, so reverse sign)
       - Render HTML table:
         - thead: Home team name | Stat | Away team name
         - tbody: map over stats array
         - For each row: homeValue cell (with font-bold if compareValue > 0), stat label center-aligned, awayValue cell (with font-bold if compareValue < 0)
       - Tailwind styling: w-full, border, responsive text sizing, gray text for labels
       - Pattern from RESEARCH.md Code Example "Team Stats Comparison Table"

    2. Tests already created in Plan 04-00, now implement to turn GREEN:
       - Test: all 10 stats are displayed in correct order
       - Test: shooting stats show "made-attempted, percentage%" format
       - Test: rebound labels use full words (not abbreviations)
       - Test: better stat value is bolded (verify font-bold class presence)
       - Test: handles tied stats (no bold when equal)
       - Test: turnovers comparison (lower is better, so away team bold when away has fewer)
       - Test: team names appear in table header
       - Use React Testing Library: render, screen.getByText, expect element to have class

    Note: Custom component (not TanStack Table) per RESEARCH.md - simpler for fixed side-by-side comparison layout.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="components/team-stats-table" -x</automated>
  </verify>
  <done>TeamStatsTable component displays all 10 stats in side-by-side format, shooting stats show made-attempted + percentage, better values are bolded, full words for rebound labels, tests turn GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate TeamStatsTable into GameDetailModal</name>
  <files>
    src/components/game-detail-modal.tsx
    tests/integration/game-detail-modal.test.tsx
  </files>
  <behavior>
    - Modal displays team stats table after score header
    - Table scrolls naturally with modal content (not sticky)
    - Empty state handled if teamStats is undefined/null
    - Integration tests verify full flow from card click to viewing team stats
  </behavior>
  <action>
    1. Modify src/components/game-detail-modal.tsx:
       - Import TeamStatsTable
       - In the data display section (after score/teams header, where placeholder "Team statistics coming in Plan 02" was):
         - Remove placeholder text
         - Add section heading: &lt;h2 className="text-xl font-bold mt-6 mb-4"&gt;Team Statistics&lt;/h2&gt;
         - Render TeamStatsTable if data.teamStats exists:
           ```tsx
           {data.teamStats &amp;&amp; (
             &lt;TeamStatsTable
               homeStats={data.teamStats.home}
               awayStats={data.teamStats.away}
               homeTeam={data.homeTeam}
               awayTeam={data.awayTeam}
             /&gt;
           )}
           ```
         - If !data.teamStats, show fallback: &lt;p className="text-gray-600"&gt;Team statistics unavailable&lt;/p&gt;
       - Keep placeholders for player stats and historical data (will be replaced in Plans 03 and 04)

    2. Tests already created in Plan 04-00, now implement to turn GREEN:
       - Test: open modal for live game, verify team stats section heading appears
       - Test: verify all 10 stats are visible in modal (check for "Field Goal Percentage", "Assists", "Total Rebounds", etc.)
       - Test: verify side-by-side format (both team names in table)
       - Test: verify shooting percentage format appears (contains "-" and "%" characters)
       - Test: click close, reopen same game, stats still display (verify no data loss)

    Note: Team stats table scrolls naturally per CONTEXT.md decision (not sticky/fixed).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="integration/game-detail-modal" -x</automated>
  </verify>
  <done>TeamStatsTable integrated into modal after score header, all 10 stats visible when modal opens, integration tests turn GREEN verifying full user flow from card click to viewing team statistics, fallback message if data unavailable</done>
</task>

</tasks>

<verification>
Overall plan verification:

1. Open modal for live game → team statistics section appears below score
2. Verify all 10 stats are displayed: FG%, 3P%, FT%, Assists, Turnovers, Offensive Rebounds, Defensive Rebounds, Total Rebounds, Steals, Blocks
3. Verify shooting stats format: "38-82, 46.3%" (made-attempted, percentage)
4. Verify better values are bolded (e.g., if home team has higher FG%, home value is bold)
5. Verify rebound labels use full words (not "OREB", but "Offensive Rebounds")
6. Verify table is side-by-side format (Home | Stat | Away columns)
7. Scroll modal down → team stats table scrolls away (not sticky)

Automated: `npm test -- --testPathPattern="04-02" -x` (all Task 1-3 tests)
</verification>

<success_criteria>
- User can view team field goal percentage with made-attempted format (STAT-01)
- User can view team three-point percentage and free throw percentage (STAT-01)
- User can view team assists, turnovers, rebounds, steals, and blocks (STAT-01, STAT-02)
- User can view turnovers specifically highlighted in the table (STAT-02)
- Better stat values are bolded for quick visual comparison
- Rebound breakdowns show offensive, defensive, and total with full word labels
- All 10 specified stats appear in correct order
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-details-statistics/04-02-SUMMARY.md`
</output>
