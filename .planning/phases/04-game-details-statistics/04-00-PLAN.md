---
phase: 04-game-details-statistics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/types/sports-data.test.ts
  - tests/lib/adapters/base-adapter.test.ts
  - tests/components/game-detail-modal.test.tsx
  - tests/components/team-stats-table.test.tsx
  - tests/components/player-stats-table.test.tsx
  - tests/components/historical-matchup.test.tsx
  - tests/integration/game-detail-modal.test.tsx
  - tests/hooks/use-game-details.test.tsx
  - tests/hooks/use-modal-history.test.tsx
autonomous: true
requirements:
  - NAV-04
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - HIST-03
  - HIST-04
  - STAT-05
user_setup: []

must_haves:
  truths:
    - "All test files exist with RED tests before implementation begins"
    - "Test commands in later plans execute successfully"
    - "Nyquist compliance achieved for Phase 04"
  artifacts:
    - path: "tests/types/sports-data.test.ts"
      provides: "RED tests for GameDetails, TeamStats, PlayerStats, HistoricalMatchup interfaces"
      min_lines: 20
    - path: "tests/lib/adapters/base-adapter.test.ts"
      provides: "RED tests for BaseAdapter.getGameDetails method"
      min_lines: 15
    - path: "tests/components/game-detail-modal.test.tsx"
      provides: "RED tests for modal open/close, dismissal methods, loading states"
      min_lines: 40
    - path: "tests/components/team-stats-table.test.tsx"
      provides: "RED tests for 10 stats display, bold highlighting, shooting format"
      min_lines: 30
    - path: "tests/components/player-stats-table.test.tsx"
      provides: "RED tests for sorting, made-attempted format, DNP handling"
      min_lines: 40
    - path: "tests/components/historical-matchup.test.tsx"
      provides: "RED tests for last 5 meetings, season series, unavailable fallback"
      min_lines: 30
    - path: "tests/integration/game-detail-modal.test.tsx"
      provides: "RED integration tests for full user flow"
      min_lines: 40
    - path: "tests/hooks/use-game-details.test.tsx"
      provides: "RED tests for on-demand fetch, loading, error handling"
      min_lines: 25
    - path: "tests/hooks/use-modal-history.test.tsx"
      provides: "RED tests for pushState, popstate listener, cleanup"
      min_lines: 20
  key_links:
    - from: "tests/*"
      to: "npm test commands in Plans 01-04"
      via: "test file existence enables automated verification"
      pattern: "npm test.*testPathPattern"
---

<objective>
Create all test files with RED tests for Phase 04 to achieve Nyquist compliance before implementation begins.

Purpose: Enable automated verification in every task across Plans 01-04, following TDD RED-GREEN-REFACTOR cycle.
Output: Complete test scaffold with failing tests that document expected behaviors for all Phase 04 components.
</objective>

<execution_context>
@/Users/bilalashraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bilalashraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-details-statistics/04-CONTEXT.md
@.planning/phases/04-game-details-statistics/04-RESEARCH.md
@.planning/phases/04-game-details-statistics/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED tests for data types and adapters</name>
  <files>
    tests/types/sports-data.test.ts
    tests/lib/adapters/base-adapter.test.ts
  </files>
  <action>
    1. Create tests/types/sports-data.test.ts:
       - Import type definitions (will fail since types don't exist yet)
       - Test: GameDetails interface has required fields (gameId, league, homeTeam, awayTeam, teamStats, playerStats, historicalMatchup)
       - Test: TeamStats interface has all 10 fields (fieldGoals, threePointers, freeThrows, assists, turnovers, reboundsOffensive, reboundsDefensive, reboundsTotal, steals, blocks)
       - Test: PlayerStats interface has all fields (jerseyNumber, lastName, firstName, minutes, points, fieldGoals, threePointers, freeThrows, rebounds, assists, steals, blocks)
       - Test: HistoricalMatchup interface has optional fields (lastFiveMeetings, seasonSeries, allTimeRecord, averageCombinedPoints)
       - Use type assertions to verify structure: `const data: GameDetails = {...}; expect(data).toBeDefined();`
       - Tests MUST fail with "Cannot find name 'GameDetails'" etc.

    2. Create tests/lib/adapters/base-adapter.test.ts:
       - Import BaseAdapter (will fail since abstract method doesn't exist yet)
       - Test: BaseAdapter has abstract getGameDetails method
       - Test: getGameDetails method signature accepts (gameId: string, league: League) and returns Promise<GameDetails>
       - Create mock adapter class extending BaseAdapter to test abstract method requirement
       - Tests MUST fail with "Property 'getGameDetails' does not exist on type 'BaseAdapter'"

    3. Run tests to verify RED state:
       ```bash
       npm test -- --testPathPattern="types/sports-data|adapters/base-adapter" -x
       ```
       Expected: All tests fail (RED state)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="types/sports-data|adapters/base-adapter" -x 2>&1 | grep -q "FAIL" && echo "RED tests created successfully"</automated>
  </verify>
  <done>Test files exist with RED tests documenting expected type structures and adapter methods, tests fail as expected</done>
</task>

<task type="auto">
  <name>Task 2: Create RED tests for modal, hooks, and skeleton components</name>
  <files>
    tests/components/game-detail-modal.test.tsx
    tests/hooks/use-game-details.test.tsx
    tests/hooks/use-modal-history.test.tsx
  </files>
  <action>
    1. Create tests/hooks/use-game-details.test.tsx:
       - Import useGameDetails hook (will fail since hook doesn't exist yet)
       - Test: hook returns { data: null, loading: true, error: null } initially when enabled=true
       - Test: hook fetches from /api/games/{league}/{gameId}/details when enabled=true
       - Test: hook returns data after successful fetch
       - Test: hook returns error after failed fetch
       - Test: hook does not fetch when enabled=false
       - Use @testing-library/react-hooks or renderHook from @testing-library/react
       - Mock fetch calls with jest.spyOn(global, 'fetch')
       - Tests MUST fail with "Cannot find module 'src/hooks/use-game-details'"

    2. Create tests/hooks/use-modal-history.test.tsx:
       - Import useModalHistory hook (will fail since hook doesn't exist yet)
       - Test: hook calls window.history.pushState when isOpen changes to true
       - Test: hook adds popstate event listener when mounted with isOpen=true
       - Test: hook calls onClose when popstate event fires
       - Test: hook removes event listener on unmount
       - Mock window.history.pushState and window.addEventListener
       - Tests MUST fail with "Cannot find module 'src/hooks/use-modal-history'"

    3. Create tests/components/game-detail-modal.test.tsx:
       - Import GameDetailModal component (will fail since component doesn't exist yet)
       - Test: modal renders with controlled open state
       - Test: modal calls onOpenChange(false) when X button clicked
       - Test: modal calls onOpenChange(false) when ESC key pressed (Radix Dialog behavior)
       - Test: modal calls onOpenChange(false) when backdrop clicked (Radix Dialog behavior)
       - Test: modal shows GameDetailSkeleton while loading
       - Test: modal shows error message when error occurs
       - Test: modal shows game data when loaded
       - Test: modal integrates useModalHistory for back button support
       - Use React Testing Library: render, screen, userEvent, waitFor
       - Tests MUST fail with "Cannot find module 'src/components/game-detail-modal'"

    4. Run tests to verify RED state:
       ```bash
       npm test -- --testPathPattern="hooks/use-game-details|hooks/use-modal-history|components/game-detail-modal" -x
       ```
       Expected: All tests fail (RED state)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="hooks/use-game-details|hooks/use-modal-history|components/game-detail-modal" -x 2>&1 | grep -q "FAIL" && echo "RED tests created successfully"</automated>
  </verify>
  <done>Test files exist with RED tests for hooks and modal component, tests fail as expected documenting expected behaviors</done>
</task>

<task type="auto">
  <name>Task 3: Create RED tests for stats tables and integration</name>
  <files>
    tests/components/team-stats-table.test.tsx
    tests/components/player-stats-table.test.tsx
    tests/components/historical-matchup.test.tsx
    tests/integration/game-detail-modal.test.tsx
  </files>
  <action>
    1. Create tests/components/team-stats-table.test.tsx:
       - Import TeamStatsTable component (will fail since component doesn't exist yet)
       - Test: all 10 stats are displayed in correct order (FG%, 3P%, FT%, AST, TO, OREB, DREB, TREB, STL, BLK)
       - Test: shooting stats show "made-attempted, percentage%" format
       - Test: rebound labels use full words (not abbreviations)
       - Test: better stat value is bolded (verify font-bold class)
       - Test: handles tied stats (no bold when equal)
       - Test: turnovers comparison (lower is better)
       - Test: team names appear in table header
       - Create mock TeamStats data for testing
       - Tests MUST fail with "Cannot find module 'src/components/team-stats-table'"

    2. Create tests/components/player-stats-table.test.tsx:
       - Import PlayerStatsTable component (will fail since component doesn't exist yet)
       - Test: all columns are displayed (Player, MIN, PTS, FG, 3P, FT, REB, AST, STL, BLK)
       - Test: default sort by points descending
       - Test: clicking PTS header toggles sort order
       - Test: clicking REB header sorts by rebounds
       - Test: shooting stats display as "made-attempted" format
       - Test: player names display as "#23 James" format
       - Test: DNP players appear with 0:00 minutes
       - Test: visual divider appears between teams
       - Test: horizontal scroll wrapper exists (overflow-x-auto class)
       - Test: sort indicators appear
       - Create mock PlayerStats arrays for home and away teams
       - Tests MUST fail with "Cannot find module 'src/components/player-stats-table'"

    3. Create tests/components/historical-matchup.test.tsx:
       - Import HistoricalMatchup component (will fail since component doesn't exist yet)
       - Test: displays "Last 5 Meetings" heading when data available
       - Test: all 5 meetings appear in list
       - Test: dates are formatted correctly
       - Test: scores appear for each meeting
       - Test: winner is indicated
       - Test: season series summary appears
       - Test: all-time record summary appears
       - Test: average combined points appears
       - Test: fallback message when data is null
       - Test: fallback message when lastFiveMeetings is empty array
       - Create mock HistoricalMatchup data for testing
       - Tests MUST fail with "Cannot find module 'src/components/historical-matchup'"

    4. Create tests/integration/game-detail-modal.test.tsx:
       - Import necessary components and render page with GameCard
       - Test: render page with live game, click game card, modal opens
       - Test: modal shows game info from selected card
       - Test: click X button, modal closes
       - Test: click backdrop, modal closes
       - Test: simulate ESC key, modal closes
       - Test: simulate back button (popstate), modal closes
       - Test: SSE connection remains active
       - Test: team stats section appears in modal
       - Test: player stats section appears in modal
       - Test: historical matchup section appears in modal
       - Test: sorting player stats works
       - Mock fetch for GameDetails API
       - Tests MUST fail with module import errors or missing components

    5. Run tests to verify RED state:
       ```bash
       npm test -- --testPathPattern="components/team-stats-table|components/player-stats-table|components/historical-matchup|integration/game-detail-modal" -x
       ```
       Expected: All tests fail (RED state)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="components/team-stats-table|components/player-stats-table|components/historical-matchup|integration/game-detail-modal" -x 2>&1 | grep -q "FAIL" && echo "RED tests created successfully"</automated>
  </verify>
  <done>Test files exist with RED tests for all stats components and integration flow, tests fail as expected documenting all expected behaviors</done>
</task>

</tasks>

<verification>
Overall Wave 0 verification:

1. All 9 test files exist in tests/ directory
2. Run `npm test -- --testPathPattern="04-" -x` → all tests FAIL (RED state)
3. Each test file documents expected behaviors for components/hooks/types
4. Test commands in Plans 01-04 now have files to execute against
5. Nyquist compliance achieved: every task can run automated verification

Automated: `npm test -- --testPathPattern="types/sports-data|adapters/base-adapter|hooks/use-game-details|hooks/use-modal-history|components/game-detail-modal|components/team-stats-table|components/player-stats-table|components/historical-matchup|integration/game-detail-modal" -x`

Expected: All tests FAIL (RED state confirms test scaffold is complete)
</verification>

<success_criteria>
- All 9 test files exist with RED tests
- Tests document expected behaviors for all Phase 04 features
- Test commands in Plans 01-04 can execute (files exist)
- Full test suite fails (RED state) as expected before implementation
- Nyquist compliance achieved: automated verification available for every task
- VALIDATION.md Wave 0 Requirements fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-details-statistics/04-00-SUMMARY.md`
</output>
