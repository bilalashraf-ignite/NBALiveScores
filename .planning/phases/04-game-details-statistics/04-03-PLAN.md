---
phase: 04-game-details-statistics
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/types/sports-data.ts
  - src/components/player-stats-table.tsx
  - src/components/game-detail-modal.tsx
  - src/lib/adapters/balldontlie-adapter.ts
  - src/lib/adapters/ncaa-adapter.ts
  - src/lib/adapters/euroleague-adapter.ts
  - package.json
  - tests/types/sports-data.test.ts
  - tests/components/player-stats-table.test.tsx
  - tests/integration/game-detail-modal.test.tsx
autonomous: true
requirements:
  - STAT-03
  - STAT-04
user_setup: []

must_haves:
  truths:
    - "User can view individual player statistics with points, rebounds, assists"
    - "User can sort player statistics by any column (PTS, REB, AST, etc.)"
    - "Players from both teams appear in single table with visual divider"
    - "DNP players are shown with 0:00 minutes"
  artifacts:
    - path: "src/types/sports-data.ts"
      provides: "PlayerStats interface with all specified stats"
      contains: "interface PlayerStats"
    - path: "src/components/player-stats-table.tsx"
      provides: "Sortable player stats table using TanStack Table"
      min_lines: 100
    - path: "tests/components/player-stats-table.test.tsx"
      provides: "Tests for sorting, made-attempted format, DNP handling, horizontal scroll"
      exports: []
  key_links:
    - from: "src/components/game-detail-modal.tsx"
      to: "PlayerStatsTable component"
      via: "renders table when data available"
      pattern: "&lt;PlayerStatsTable.*playerStats"
    - from: "src/components/player-stats-table.tsx"
      to: "TanStack Table v8"
      via: "useReactTable hook"
      pattern: "useReactTable.*getSortedRowModel"
---

<objective>
Create comprehensive player statistics table with sortable columns, both teams displayed with divider, made-attempted format for shooting stats, and horizontal scroll for mobile.

Purpose: Enable users to view detailed player performance and compare across teams (STAT-03, STAT-04).
Output: Sortable player statistics table showing points, rebounds, assists, shooting stats, and defensive stats for all players.
</objective>

<execution_context>
@/Users/bilalashraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bilalashraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-details-statistics/04-CONTEXT.md
@.planning/phases/04-game-details-statistics/04-RESEARCH.md
@.planning/phases/04-game-details-statistics/04-VALIDATION.md
@.planning/phases/04-game-details-statistics/04-01-PLAN.md
@.planning/phases/04-game-details-statistics/04-02-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/sports-data.ts (created in Plan 01, extended in Plan 02):
```typescript
export interface GameDetails {
  gameId: string;
  league: League;
  homeTeam: Team;
  awayTeam: Team;
  teamStats: { home: TeamStats; away: TeamStats };
  playerStats: {
    home: PlayerStats[];  // Will define PlayerStats in this plan
    away: PlayerStats[];
  };
  historicalMatchup: HistoricalMatchup;
}
```

From CONTEXT.md Player Statistics section:
- Points, Field Goals (made-attempted), Three-Pointers (made-attempted), Free Throws (made-attempted)
- Rebounds, Assists, Steals, Blocks
- Minutes played (format: "32:15")
- Jersey number + last name format (e.g., "#23 James")
- DNP players shown with 0:00 minutes
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define PlayerStats interface and populate mock data</name>
  <files>
    src/types/sports-data.ts
    src/lib/adapters/balldontlie-adapter.ts
    src/lib/adapters/ncaa-adapter.ts
    src/lib/adapters/euroleague-adapter.ts
    tests/types/sports-data.test.ts
  </files>
  <behavior>
    - PlayerStats interface includes all specified stats per CONTEXT.md
    - Shooting stats have { made: number, attempted: number } structure
    - Minutes field is string format "MM:SS"
    - Mock adapters return GameDetails with realistic player rosters (10-12 players per team)
    - Include 1-2 DNP players with 0:00 minutes and zero stats
  </behavior>
  <action>
    1. Add PlayerStats interface to src/types/sports-data.ts:
       ```typescript
       export interface PlayerStats {
         jerseyNumber: string;      // e.g., "23"
         lastName: string;          // e.g., "James"
         firstName: string;         // For full name on hover/accessibility
         minutes: string;           // Format: "32:15" or "0:00" for DNP
         points: number;
         fieldGoals: { made: number; attempted: number };
         threePointers: { made: number; attempted: number };
         freeThrows: { made: number; attempted: number };
         rebounds: number;
         assists: number;
         steals: number;
         blocks: number;
       }
       ```

    2. Update BalldontlieAdapter.getGameDetails() with realistic NBA player stats:
       - Home team (Lakers): 12 players including starters like LeBron James (#23, 35 min, 28 pts, 8-15 FG, 2-5 3P, 10-12 FT, 7 REB, 9 AST), Anthony Davis (#3, 33 min, 24 pts), bench players, 1 DNP
       - Away team (Celtics): 12 players including Jayson Tatum (#0, 36 min, 32 pts), Jaylen Brown (#7, 34 min, 22 pts), bench players, 1 DNP
       - Realistic stat distributions: starters 25-35 min, bench 10-20 min, DNP 0:00
       - Points range: star players 25-35 pts, starters 10-20 pts, bench 5-10 pts, DNP 0 pts

    3. Update NcaaAdapter with realistic NCAA player roster (Duke vs UNC)

    4. Update EuroLeagueAdapter with realistic EuroLeague player roster (Real Madrid vs Barcelona)

    Note: Mock data per CONTEXT.md decision (defer real API integration).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="types/sports-data" -x</automated>
  </verify>
  <done>PlayerStats interface defined with all specified stats, adapters return GameDetails with realistic player rosters including DNP players, tests verify structure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PlayerStatsTable component with TanStack Table sorting</name>
  <files>
    src/components/player-stats-table.tsx
    package.json
    tests/components/player-stats-table.test.tsx
  </files>
  <behavior>
    - Table displays both teams' players separated by visual divider (home first, divider, away)
    - Sortable columns: Player, MIN, PTS, FG, 3P, FT, REB, AST, STL, BLK
    - Default sort: by points descending (highest scorers first)
    - Shooting stats display as "made-attempted" (e.g., "8-15")
    - Player names display as "#jerseyNumber lastName" (e.g., "#23 James")
    - DNP players included with 0:00 minutes
    - Horizontal scroll wrapper for mobile (all columns remain visible)
  </behavior>
  <action>
    1. Install TanStack Table:
       ```bash
       npm install @tanstack/react-table
       ```

    2. Create src/components/player-stats-table.tsx:
       - Import useReactTable, getCoreRowModel, getSortedRowModel, ColumnDef from @tanstack/react-table
       - Accept props: homeStats: PlayerStats[], awayStats: PlayerStats[], homeTeam: Team, awayTeam: Team
       - Combine data with team identifier: `const data = [...homeStats.map(p => ({ ...p, team: 'home' })), ...awayStats.map(p => ({ ...p, team: 'away' }))]`
       - Define column definitions (ColumnDef&lt;PlayerStats&gt;[]):
         - Player: accessorFn returns lastName, cell displays "#jerseyNumber lastName", enableSorting: false
         - MIN: accessorKey 'minutes', sortingFn: 'alphanumeric'
         - PTS: accessorKey 'points', sortDescFirst: true, sortUndefined: 'last'
         - FG: accessorFn returns fieldGoals.made, cell displays "made-attempted", sortDescFirst: true
         - 3P: accessorFn returns threePointers.made, cell displays "made-attempted", sortDescFirst: true
         - FT: accessorFn returns freeThrows.made, cell displays "made-attempted", sortDescFirst: true
         - REB: accessorKey 'rebounds', sortDescFirst: true, sortUndefined: 'last'
         - AST: accessorKey 'assists', sortDescFirst: true, sortUndefined: 'last'
         - STL: accessorKey 'steals', sortDescFirst: true, sortUndefined: 'last'
         - BLK: accessorKey 'blocks', sortDescFirst: true, sortUndefined: 'last'
       - Set initial sorting state: `const [sorting, setSorting] = useState([{ id: 'points', desc: true }])`
       - Call useReactTable with: data, columns, state: { sorting }, onSortingChange: setSorting, getCoreRowModel, getSortedRowModel
       - Render table structure:
         - Wrapper div with overflow-x-auto for horizontal scroll
         - Table with min-w-full, border styling
         - thead: map headerGroups, for each header add onClick handler (getToggleSortingHandler), show sort indicator (▲ ▼)
         - tbody: map rows, add visual divider row between home and away teams (detect when team changes from 'home' to 'away')
         - Divider row: colSpan={columns.length}, border-t-2 border-gray-400, text-center showing away team name
         - Tailwind: text-sm, whitespace-nowrap for cells, hover:bg-gray-50 for rows
       - Pattern from RESEARCH.md Code Example "TanStack Table Column Definitions for Player Stats"

    3. Create tests/components/player-stats-table.test.tsx:
       - Test: all columns are displayed (Player, MIN, PTS, FG, 3P, FT, REB, AST, STL, BLK)
       - Test: default sort by points descending (highest scorer first)
       - Test: clicking PTS header toggles sort order (desc → asc → desc)
       - Test: clicking REB header sorts by rebounds
       - Test: shooting stats display as "made-attempted" format (e.g., "8-15")
       - Test: player names display as "#23 James" format
       - Test: DNP players appear with 0:00 minutes and zero stats
       - Test: visual divider appears between home and away teams
       - Test: both team names appear (in divider or as section labels)
       - Test: horizontal scroll wrapper exists (check for overflow-x-auto class)
       - Test: sort indicators (▲ ▼) appear when column is sorted
       - Use React Testing Library: render, screen.getAllByRole('columnheader'), userEvent.click, expect element order

    Note: TanStack Table per RESEARCH.md standard stack (handles edge cases for sorting, DNP players with sortUndefined: 'last').
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="components/player-stats-table" -x</automated>
  </verify>
  <done>PlayerStatsTable component displays both teams with divider, all columns sortable, default sort by points, shooting stats as made-attempted format, player names as "#N Name", DNP players included, horizontal scroll wrapper, tests verify behavior</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate PlayerStatsTable into GameDetailModal</name>
  <files>
    src/components/game-detail-modal.tsx
    tests/integration/game-detail-modal.test.tsx
  </files>
  <behavior>
    - Modal displays player stats table after team stats section
    - Table is scrollable within modal (not fixed)
    - Empty state handled if playerStats is undefined/null
    - Integration tests verify sorting interaction and full data flow
  </behavior>
  <action>
    1. Modify src/components/game-detail-modal.tsx:
       - Import PlayerStatsTable
       - In the data display section (after team stats, where placeholder "Player statistics coming in Plan 03" was):
         - Remove placeholder text
         - Add section heading: &lt;h2 className="text-xl font-bold mt-6 mb-4"&gt;Player Statistics&lt;/h2&gt;
         - Render PlayerStatsTable if data.playerStats exists:
           ```tsx
           {data.playerStats &amp;&amp; (
             &lt;PlayerStatsTable
               homeStats={data.playerStats.home}
               awayStats={data.playerStats.away}
               homeTeam={data.homeTeam}
               awayTeam={data.awayTeam}
             /&gt;
           )}
           ```
         - If !data.playerStats, show fallback: &lt;p className="text-gray-600"&gt;Player statistics unavailable&lt;/p&gt;
       - Keep placeholder for historical data (will be replaced in Plan 04)

    2. Update tests/integration/game-detail-modal.test.tsx:
       - Test: open modal for live game, verify player stats section heading appears
       - Test: verify player names appear in table (check for "#23" pattern)
       - Test: verify all column headers appear (Player, MIN, PTS, FG, 3P, FT, REB, AST, STL, BLK)
       - Test: verify both teams appear (check for visual divider or team names)
       - Test: click PTS column header, verify sort order changes (use data-testid or aria-sort attribute)
       - Test: verify made-attempted format appears in FG column (contains "-" character)
       - Test: scroll modal vertically, player stats table scrolls with content (not sticky)
       - Test: DNP players visible (check for "0:00" in minutes column)

    Note: Player stats table scrolls naturally per CONTEXT.md decision (not sticky/fixed).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="integration/game-detail-modal" -x</automated>
  </verify>
  <done>PlayerStatsTable integrated into modal after team stats, all columns sortable via click, both teams visible with divider, DNP players included, integration tests verify sorting and full user flow from card click to viewing/sorting player statistics</done>
</task>

</tasks>

<verification>
Overall plan verification:

1. Open modal for live game → player statistics section appears below team stats
2. Verify all player columns: Player, MIN, PTS, FG, 3P, FT, REB, AST, STL, BLK
3. Verify both teams appear with visual divider between them
4. Verify default sort: highest scorers first (points descending)
5. Click PTS column header → sort toggles (asc/desc)
6. Click REB column header → table re-sorts by rebounds
7. Verify shooting stats format: "8-15" (made-attempted)
8. Verify player names format: "#23 James"
9. Verify DNP players appear with 0:00 minutes
10. Narrow browser viewport → table horizontal scrolls (all columns remain visible)

Automated: `npm test -- --testPathPattern="04-03" -x` (all Task 1-3 tests)
</verification>

<success_criteria>
- User can view individual player statistics during live games (STAT-03)
- User can see player points, rebounds, and assists (STAT-04)
- User can sort by any column (PTS, REB, AST, etc.) by clicking headers
- Players from both teams appear in single table separated by visual divider
- Default sort by points (highest scorers first) highlights top performers
- Shooting stats display as made-attempted format (e.g., "8-15 FG")
- Player names display as "#jerseyNumber lastName" format
- DNP players included with 0:00 minutes
- Horizontal scroll enables viewing all columns on mobile devices
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-details-statistics/04-03-SUMMARY.md`
</output>
