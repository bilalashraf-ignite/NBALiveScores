---
phase: 05-performance-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/player-stats-table.tsx
autonomous: true
requirements:
  - PERF-01
gap_closure: true
must_haves:
  truths:
    - "Player stats table has proper light/dark mode color scheme suitable for presentations"
    - "Table text is readable with high contrast in both light and dark modes"
  artifacts:
    - path: "src/components/player-stats-table.tsx"
      provides: "Fixed light/dark mode color scheme"
      contains: "bg-white dark:bg-gray-800"
  key_links:
    - from: "player-stats-table.tsx"
      to: "globals.css dark mode media query"
      via: "Tailwind dark: classes"
      pattern: "dark:bg-gray-800"
---

<objective>
Fix player stats table color scheme to match proper light/dark mode patterns used throughout the application.

Purpose: Resolve cosmetic issue where dark backgrounds with light text appear on white modal background in light mode, making the table nearly unreadable during presentations and normal use.

Output: Player stats table with consistent color scheme matching game cards, team stats table, and modal background patterns.
</objective>

<execution_context>
@/Users/bilalashraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bilalashraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/color-scheme-unclear.md
@.planning/phases/05-performance-polish/05-00-SUMMARY.md
@.planning/phases/05-performance-polish/05-04-SUMMARY.md
</context>

<interfaces>
<!-- Pattern established in other components -->

From src/components/game-card.tsx (CORRECT pattern):
```tsx
// Light mode: white background, dark text
// Dark mode: dark background, light text
className="bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100"
```

From src/components/team-stats-table.tsx (CORRECT pattern):
```tsx
// Table headers
className="bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100"

// Table cells
className="text-gray-900 dark:text-gray-100"
```

Current BROKEN pattern in player-stats-table.tsx:
```tsx
// Line 164: Team divider
className="bg-gray-700" // Always dark

// Line 171: Table rows
className="bg-gray-700 dark:bg-gray-800" // Always dark

// Line 178: Cell text
className="text-gray-500 dark:text-gray-400" // Always light gray
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix player stats table color scheme</name>
  <files>src/components/player-stats-table.tsx</files>
  <action>
Update player-stats-table.tsx color scheme to match established light/dark mode pattern used in game-card.tsx and team-stats-table.tsx.

**Line 164 (team divider row):**
Change: `bg-gray-700`
To: `bg-gray-100 dark:bg-gray-700`
Reason: Light gray in light mode, dark gray in dark mode

**Line 171 (table row backgrounds):**
Change: `bg-gray-700 dark:bg-gray-800`
To: `bg-white dark:bg-gray-800`
Reason: White background in light mode (matches modal), dark in dark mode

**Line 171 (hover state):**
Keep: `hover:bg-purple-100 dark:hover:bg-purple-900/40`
Note: This works correctly now that base background is white in light mode

**Line 178 (cell text):**
Change: `text-gray-500 dark:text-gray-400`
To: `text-gray-900 dark:text-gray-100`
Reason: Dark text on white background (light mode), light text on dark background (dark mode)

**Verify pattern consistency:**
- Light mode: `bg-white` + `text-gray-900` = dark text on white (high contrast)
- Dark mode: `dark:bg-gray-800` + `dark:text-gray-100` = light text on dark (high contrast)
- Team divider: `bg-gray-100` (subtle difference from white) in light mode
- Hover: Purple tint works on both white and dark backgrounds
  </action>
  <verify>
    <automated>npm test -- player-stats-table.test.tsx</automated>
  </verify>
  <done>
Player stats table displays with:
- White background + dark text in light mode
- Dark background + light text in dark mode
- Team divider has subtle gray background (light mode) or dark gray (dark mode)
- Hover state shows purple tint on both modes
- Pattern matches game-card.tsx and team-stats-table.tsx
- Tests pass with no visual regression
  </done>
</task>

</tasks>

<verification>
**Visual check (light mode):**
- Player table has white background matching modal
- Text is dark gray/black (readable from distance)
- Team divider has light gray background
- Purple hover effect visible on white

**Visual check (dark mode):**
- Player table has dark gray background matching modal
- Text is light gray/white (readable)
- Team divider has darker gray background
- Purple hover effect visible on dark

**Pattern consistency:**
- All tables (team stats, player stats) use same color pattern
- Matches game card styling
- Matches modal background styling
</verification>

<success_criteria>
- Player stats table uses `bg-white dark:bg-gray-800` for row backgrounds
- Text uses `text-gray-900 dark:text-gray-100` for high contrast
- Team divider uses `bg-gray-100 dark:bg-gray-700`
- Light mode shows dark text on white background
- Dark mode shows light text on dark background
- Presentation mode displays table clearly without jarring contrast
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-polish/05-05-SUMMARY.md`
</output>
