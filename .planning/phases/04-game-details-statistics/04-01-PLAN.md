---
phase: 04-game-details-statistics
plan: 01
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src/components/game-detail-modal.tsx
  - src/hooks/use-game-details.ts
  - src/hooks/use-modal-history.ts
  - src/types/sports-data.ts
  - src/lib/adapters/base-adapter.ts
  - src/lib/adapters/balldontlie-adapter.ts
  - src/lib/adapters/ncaa-adapter.ts
  - src/lib/adapters/euroleague-adapter.ts
  - src/components/game-card.tsx
  - src/app/page.tsx
  - package.json
  - tests/components/game-detail-modal.test.tsx
  - tests/hooks/use-game-details.test.tsx
  - tests/hooks/use-modal-history.test.tsx
  - tests/integration/game-detail-modal.test.tsx
autonomous: true
requirements:
  - NAV-04
  - STAT-05
user_setup: []

must_haves:
  truths:
    - "User can click a game card to open detailed game view in modal"
    - "Modal opens with fade + scale animation from center"
    - "Modal can be dismissed via X button, ESC key, clicking outside, or browser back button"
    - "SSE connection remains active while modal is open"
    - "GameDetails data fetches on-demand when modal opens"
  artifacts:
    - path: "src/components/game-detail-modal.tsx"
      provides: "Modal component with Radix Dialog, dismissal handlers"
      min_lines: 80
    - path: "src/hooks/use-game-details.ts"
      provides: "On-demand GameDetails fetch hook with loading state"
      exports: ["useGameDetails"]
    - path: "src/hooks/use-modal-history.ts"
      provides: "Browser history integration for back button"
      exports: ["useModalHistory"]
    - path: "src/types/sports-data.ts"
      provides: "GameDetails interface with team/player/historical data"
      contains: "interface GameDetails"
    - path: "src/lib/adapters/base-adapter.ts"
      provides: "getGameDetails abstract method"
      contains: "abstract getGameDetails"
  key_links:
    - from: "src/components/game-card.tsx"
      to: "modal state in page.tsx"
      via: "onClick handler"
      pattern: "onClick.*setSelectedGameId"
    - from: "src/hooks/use-game-details.ts"
      to: "/api/games/{league}/{gameId}/details"
      via: "fetch call"
      pattern: "fetch.*api/games"
    - from: "src/components/game-detail-modal.tsx"
      to: "useModalHistory"
      via: "hook call for back button"
      pattern: "useModalHistory.*isOpen"
---

<objective>
Create modal infrastructure for game details display with Radix Dialog, on-demand data fetching, browser history integration, and GameCard integration.

Purpose: Enable users to access detailed game statistics by clicking game cards while maintaining SSE connection.
Output: Clickable game cards that open accessible modal dialogs with loading states and multiple dismissal methods.
</objective>

<execution_context>
@/Users/bilalashraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bilalashraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-details-statistics/04-CONTEXT.md
@.planning/phases/04-game-details-statistics/04-RESEARCH.md
@.planning/phases/04-game-details-statistics/04-VALIDATION.md
@.planning/phases/04-game-details-statistics/04-00-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/types/sports-data.ts:
```typescript
export type League = 'NBA' | 'NCAA' | 'EuroLeague';

export interface Game {
  id: string;
  league: League;
  homeTeam: Team;
  awayTeam: Team;
  status: GameStatus;
  score: Score;
  state: GameState;
  gameContext?: GameContext;
  teamFouls?: TeamFouls;
}

export interface Team {
  id: string;
  name: string;
  abbreviation: string;
  logoUrl?: string;
}
```

From src/lib/adapters/base-adapter.ts:
```typescript
export abstract class BaseAdapter {
  abstract getLiveGames(league: League): Promise<Game[]>;
  abstract getScheduledGames(league: League): Promise<Game[]>;
  // Will add: abstract getGameDetails(gameId: string, league: League): Promise<GameDetails>
}
```

From src/components/game-card.tsx:
```typescript
interface GameCardProps {
  game: Game;
  // Will add: onClick?: () => void
}

export function GameCard({ game }: GameCardProps) {
  // Card displays game info, will add click handler
}
```

Test files created in Plan 04-00:
- tests/types/sports-data.test.ts (RED tests for GameDetails interface)
- tests/lib/adapters/base-adapter.test.ts (RED tests for getGameDetails method)
- tests/hooks/use-game-details.test.tsx (RED tests for data fetching hook)
- tests/hooks/use-modal-history.test.tsx (RED tests for history integration)
- tests/components/game-detail-modal.test.tsx (RED tests for modal component)
- tests/integration/game-detail-modal.test.tsx (RED integration tests)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GameDetails data model and adapter method</name>
  <files>
    src/types/sports-data.ts
    src/lib/adapters/base-adapter.ts
    src/lib/adapters/balldontlie-adapter.ts
    src/lib/adapters/ncaa-adapter.ts
    src/lib/adapters/euroleague-adapter.ts
    tests/types/sports-data.test.ts
    tests/lib/adapters/base-adapter.test.ts
  </files>
  <behavior>
    - GameDetails interface includes gameId, league, team info, score, and placeholders for teamStats, playerStats, historicalMatchup
    - BaseAdapter has abstract getGameDetails(gameId: string, league: League): Promise&lt;GameDetails&gt; method
    - Each league adapter implements getGameDetails returning mock GameDetails data
    - Mock data includes realistic game info with league field set correctly
  </behavior>
  <action>
    1. Add GameDetails interface to src/types/sports-data.ts:
       - gameId: string
       - league: League
       - homeTeam, awayTeam: Team (reuse existing Team interface)
       - status: GameStatus
       - score: Score
       - gameContext?: GameContext
       - teamFouls?: TeamFouls
       - teamStats: { home: TeamStats, away: TeamStats } (placeholder object for now, will define TeamStats interface in Plan 02)
       - playerStats: { home: PlayerStats[], away: PlayerStats[] } (placeholder array for now, will define PlayerStats interface in Plan 03)
       - historicalMatchup: HistoricalMatchup (placeholder object for now, will define HistoricalMatchup interface in Plan 04)

    2. Add abstract getGameDetails method to BaseAdapter class

    3. Implement getGameDetails in BalldontlieAdapter (NBA):
       - Returns mock GameDetails with league='NBA'
       - Include realistic game info (Lakers vs Celtics, live game with score)
       - teamStats, playerStats, historicalMatchup as empty objects/arrays (will populate in later plans)

    4. Implement getGameDetails in NcaaAdapter:
       - Returns mock GameDetails with league='NCAA'
       - Include realistic NCAA game (Duke vs UNC)

    5. Implement getGameDetails in EuroLeagueAdapter:
       - Returns mock GameDetails with league='EuroLeague'
       - Include realistic EuroLeague game (Real Madrid vs Barcelona)

    Note: Using mock data per CONTEXT.md locked decision (defer real API integration).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="types/sports-data|adapters/base-adapter" -x</automated>
  </verify>
  <done>GameDetails interface exists in sports-data.ts, BaseAdapter has abstract getGameDetails method, all three adapters implement getGameDetails with mock data, tests verify structure and turn GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useGameDetails hook and Radix Dialog modal component</name>
  <files>
    src/hooks/use-game-details.ts
    src/hooks/use-modal-history.ts
    src/components/game-detail-modal.tsx
    src/components/game-detail-skeleton.tsx
    package.json
    tests/hooks/use-game-details.test.tsx
    tests/hooks/use-modal-history.test.tsx
    tests/components/game-detail-modal.test.tsx
  </files>
  <behavior>
    - useGameDetails fetches GameDetails on-demand when enabled=true
    - Returns { data, loading, error } state
    - useModalHistory pushes history state when modal opens, listens for popstate for back button
    - GameDetailModal uses Radix Dialog with controlled open state
    - Modal shows skeleton loader while loading
    - Modal has X button, backdrop click, ESC key dismissal (Radix Dialog handles ESC and backdrop automatically)
    - Browser back button closes modal via useModalHistory
  </behavior>
  <action>
    1. Install Radix Dialog:
       ```bash
       npm install @radix-ui/react-dialog
       ```

    2. Create src/hooks/use-game-details.ts:
       - Accept gameId: string, league: League, enabled: boolean params
       - Fetch from /api/games/{league}/{gameId}/details when enabled=true
       - Return { data: GameDetails | null, loading: boolean, error: Error | null }
       - Use useEffect to trigger fetch when enabled changes
       - Pattern from RESEARCH.md Pattern 4 (on-demand data fetching)

    3. Create src/hooks/use-modal-history.ts:
       - Accept isOpen: boolean, onClose: () =&gt; void params
       - useEffect: when isOpen changes to true, call window.history.pushState({ modal: true }, '')
       - Add popstate event listener that calls onClose if isOpen=true
       - Cleanup: remove event listener on unmount
       - Pattern from RESEARCH.md Pattern 3 (browser history integration)

    4. Create src/components/game-detail-skeleton.tsx:
       - Reuse pattern from GameCardSkeleton (animate-pulse on gray rectangles)
       - Structure: score header skeleton (h-20), team stats skeleton (3-4 rows), player stats skeleton (5-6 rows)
       - Single scrollable container matching final modal layout

    5. Create src/components/game-detail-modal.tsx:
       - Import Radix Dialog components: Dialog.Root, Dialog.Portal, Dialog.Overlay, Dialog.Content, Dialog.Title, Dialog.Description, Dialog.Close
       - Accept props: gameId: string, league: League, open: boolean, onOpenChange: (open: boolean) =&gt; void
       - Call useGameDetails(gameId, league, open) to fetch on-demand
       - Call useModalHistory(open, () =&gt; onOpenChange(false)) for back button
       - Render Dialog.Root with controlled open state
       - Dialog.Overlay with fixed inset-0 bg-black/50
       - Dialog.Content with fixed positioning, centered transform, white bg, rounded corners, max-w-4xl, max-h-[90vh], overflow-y-auto
       - Animation: Tailwind classes for fade + scale (transition-all duration-200 data-[state=open]:animate-in data-[state=closed]:animate-out)
       - Dialog.Title for accessibility (can be visually hidden)
       - Dialog.Close button in top-right with X icon
       - Content: if loading, show GameDetailSkeleton; if error, show error message; if data, show game info (score, teams) with placeholders "Team statistics coming in Plan 02", "Player statistics coming in Plan 03", "Historical data coming in Plan 04"
       - Pattern from RESEARCH.md Pattern 1 (Radix UI Dialog with controlled state)

    Note: Use RESEARCH.md's recommended approach with Radix Dialog for accessibility (focus trap, ARIA compliance built-in).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="hooks/use-game-details|hooks/use-modal-history|components/game-detail-modal" -x</automated>
  </verify>
  <done>useGameDetails hook fetches data on-demand with loading state, useModalHistory integrates browser back button, GameDetailModal renders with Radix Dialog and shows skeleton while loading, all dismissal methods work, tests turn GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate modal with GameCard and home page</name>
  <files>
    src/components/game-card.tsx
    src/app/page.tsx
    tests/components/game-card.test.tsx
    tests/integration/game-detail-modal.test.tsx
  </files>
  <behavior>
    - GameCard becomes clickable when game state is LIVE or FINAL (not SCHEDULED)
    - Clicking card triggers onClick handler passed from page
    - Home page manages selectedGameId state
    - Modal opens when selectedGameId is set
    - Closing modal clears selectedGameId
    - SSE connection remains active (no unmounting of home page)
  </behavior>
  <action>
    1. Modify src/components/game-card.tsx:
       - Add onClick?: () =&gt; void prop
       - Check canShowDetails = game.state === GameState.LIVE || game.state === GameState.FINAL
       - If canShowDetails, add:
         - onClick handler on outer div
         - cursor-pointer and hover:shadow-md classes
         - role="button" and tabIndex={0} for accessibility
         - onKeyDown handler for Enter/Space keys (accessibility)
       - If !canShowDetails, don't add click behavior
       - Pattern from RESEARCH.md Code Example "Modal Integration with GameCard Click Handler"

    2. Modify src/app/page.tsx:
       - Import GameDetailModal
       - Add state: const [selectedGameId, setSelectedGameId] = useState&lt;string | null&gt;(null)
       - Add state: const [selectedLeague, setSelectedLeague] = useState&lt;League | null&gt;(null)
       - When rendering GameCard components, pass onClick={() =&gt; { setSelectedGameId(game.id); setSelectedLeague(game.league); }}
       - After game list rendering, add:
         ```tsx
         {selectedGameId &amp;&amp; selectedLeague &amp;&amp; (
           &lt;GameDetailModal
             gameId={selectedGameId}
             league={selectedLeague}
             open={true}
             onOpenChange={(open) =&gt; {
               if (!open) {
                 setSelectedGameId(null);
                 setSelectedLeague(null);
               }
             }}
           /&gt;
         )}
         ```
       - Ensure modal is rendered outside game list container (after it) so it overlays properly

    3. Update tests/integration/game-detail-modal.test.tsx:
       - Integration test: render page with live game, click game card, verify modal opens
       - Test: modal shows game info from selected card
       - Test: click X button, modal closes, home page still shows games
       - Test: click backdrop (Dialog.Overlay), modal closes
       - Test: simulate ESC key, modal closes
       - Test: simulate back button (trigger popstate), modal closes
       - Test: verify SSE connection remains active (useSSE still connected after modal opens/closes)
       - Pattern: Use React Testing Library's waitFor, screen.getByRole, userEvent for clicks

    Note: CONTEXT.md locked decision: Modal maintains SSE connection (no URL change, no remounting).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="game-card|integration/game-detail-modal" -x</automated>
  </verify>
  <done>GameCard is clickable for LIVE/FINAL games, clicking opens modal with correct game, modal can be dismissed via all methods (X, ESC, backdrop, back), SSE remains active, integration tests turn GREEN verifying full flow</done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Click any LIVE or FINAL game card → modal opens with game info
2. Modal shows loading skeleton initially, then game details
3. Close modal via X button → modal closes, returns to home page
4. Open modal, press ESC key → modal closes
5. Open modal, click outside on backdrop → modal closes
6. Open modal, press browser back button → modal closes
7. Verify home page scores continue updating (SSE active) while modal is open and after close
8. SCHEDULED games do not open modal when clicked (cursor-default, no hover effect)

Automated: `npm test -- --testPathPattern="04-01" -x` (all Task 1-3 tests)
</verification>

<success_criteria>
- User can click LIVE or FINAL game cards to open detailed view in modal (NAV-04)
- Modal opens with smooth fade + scale animation from center
- Modal can be dismissed via X button, ESC key, clicking backdrop, or browser back button
- SSE connection remains active while modal is open (verified by continued score updates)
- GameDetails data fetches on-demand only when modal opens (verified by network tab showing fetch on click)
- Loading skeleton displays while GameDetails is fetching
- SCHEDULED games are not clickable (no cursor-pointer, no modal opens)
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-details-statistics/04-01-SUMMARY.md`
</output>
